# Leave empty for production same-origin requests. For local Vite, prefer the
# proxy target below so Secure/SameSite cookies stay on the local origin.
VITE_API_BASE_URL=
VITE_DEV_API_TARGET=http://localhost:8000
